#include <stdlib.h>
#include <stdio.h>

/**
 * main - Prints the number of arguments
 * @argc: The size of array containing the arguments
 * @argv: The array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);

	return (0);
}

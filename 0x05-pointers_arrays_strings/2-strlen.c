#include "main.h"
#include <string.h>

/**
 * _strlen - calculate the length of a string
 * @s: the char sequence or string
 * Return: the length
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}

